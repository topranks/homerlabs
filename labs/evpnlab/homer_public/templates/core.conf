{% from "macros/common.conf" import login, section with context %}
system {
    host-name {{ hostname }}
}
{{ section("core", "policy_options") }}
{{ section("core", "protocols") }}
{{ section("core", "routing_options") }}
